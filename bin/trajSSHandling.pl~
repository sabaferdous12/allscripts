use strict;
use warnings;

my $inputFile = $ARGV[0]; # Trajectory file
my ($name, $ext) = split ("\.", $inputFile);

# Extract pdb frames from trajectory every 10ps

`gmx trjconv -s input_100.tpr -f $inputFile -o $name.pdb -dt 10 -sep`;



